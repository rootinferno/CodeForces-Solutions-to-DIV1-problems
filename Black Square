#include<bits/stdc++.h>
using namespace std ;
int main()
{
  int j,n,i,sum=0;
  string s ;
  int a[5] ;
  for(i=1;i<=4;i++)
      cin>> a[i] ;
  cin>> s ;
  for(int i=0;i<s.size();i++)
      sum += a[s[i]-'0'] ;
  cout<< sum << endl;
return 0 ;
}
