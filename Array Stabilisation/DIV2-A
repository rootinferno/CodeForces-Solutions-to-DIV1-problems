#include<bits/stdc++.h>
using namespace std ;
int main()
{
    long long int n ;
    int p;
    vector <int> v ;
    cin >> n ;
    for(int i=0;i<n;i++)
    {
       cin >> p ;
       v.push_back(p) ;
    }

    sort(v.begin(),v.end()) ;
    cout << min(v[n-2]-v[0],v[n-1]-v[1]) ;
    return 0;
}
