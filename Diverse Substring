#include<iostream>
using namespace std ;

int main()
{
    int n ;
    string s ;
    cin >> n >> s ;

    for(int i = 0 ;i<n ; i++)

        if(s[0]!=s[i])
            return cout << "YES\n"<< s[0] << s[i] , 0 ;
            cout << "NO";
    }
